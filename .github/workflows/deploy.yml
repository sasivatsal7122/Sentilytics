name: Deploy to Raspberry Pi

on:
  push:
    branches:
      - rspi

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: SSH into Raspberry Pi and deploy with tailscale
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: kBxCDi1CNTRL
          oauth-secret: tskey-client-kBxCDi1CNTRL-BmZ41ivp1cFWk21vwmy3cFpJj7ZdPU9TD
          script: |
            cd /home/sasi/Sentilytics-rspi
            git pull origin rspi
            



